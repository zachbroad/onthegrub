import 'package:flutter/material.dart';

class Style {
  static final textShadowBlack = Shadow(
    color: Colors.black,
    blurRadius: 3.5,
  );

  static final textShadowWhite = Shadow(
    color: Colors.white,
    blurRadius: 1.1,
  );
}
